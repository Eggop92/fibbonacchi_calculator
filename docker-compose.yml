version: "3"
services:
  client:
    image: "eggop1992/prod-fib-client"
    mem_limit: 128m
    hostname: client
  server:
    image: "eggop1992/prod-fib-server"
    mem_limit: 128m
    hostname: api
    environment:
      - REDIS_HOST=$REDIS_HOST
      - REDIS_PORT=$REDIS_PORT
      - PGUSER=$PGUSER
      - PGHOST=$PGHOST
      - PGDATABASE=$PGDATABASE
      - PGPASSWORD=$PGPASSWORD
      - PGPORT=$PGPORT
  worker:
    image: "eggop1992/prod-fib-worker"
    mem_limit: 128m
    hostname: worker
    environment:
      - REDIS_HOST=$REDIS_HOST
      - REDIS_PORT=$REDIS_PORT
  nginx:
    image: "eggop1992/prod-fib-nginx"
    mem_limit: 128m
    hostname: nginx
    ports:
      - "80:80"
